# Plan: Commit and push upload button fix

## Summary
The upload button fix in `src/components/admin/ReportsClient.tsx` is already implemented. Need to commit and push.

## Steps
1. `git add src/components/admin/ReportsClient.tsx`
2. `git commit -m "fix: improve upload button UX in Reports, Documents & Issuances"`
3. `git push`
